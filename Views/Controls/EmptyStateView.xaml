<UserControl x:Class="CallAnalog.Softphone.Views.Controls.EmptyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:CallAnalog.Softphone.Views.Controls"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="16,20">
            <Border Width="32"
                    Height="32"
                    CornerRadius="16"
                    Background="{StaticResource PhoneSurfaceBrush}"
                    Margin="0,0,0,8"
                    HorizontalAlignment="Center"
                    Visibility="Collapsed">
                <controls:PackIcon x:Name="StateIcon"
                                   IconKey="IconDialpad"
                                   IconSize="14"
                                   Foreground="{StaticResource PhoneTextSecondaryBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
            </Border>
            <TextBlock x:Name="TitleText"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource PhoneTextPrimaryBrush}"
                       TextAlignment="Center"
                       Margin="0,0,0,6"/>
            <TextBlock x:Name="MessageText"
                       FontSize="11"
                       Foreground="{StaticResource PhoneTextSecondaryBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="220"/>
        </StackPanel>
    </Grid>
</UserControl>
